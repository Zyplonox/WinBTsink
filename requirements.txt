# Python dependencies for BT-AudioSink
#
# Installation:
#   python -m pip install -r requirements.txt
#
# Bluetooth itself is handled by btstack_sink.exe (C, BTstack), built with
# btstack\build.ps1. USB dongle enumeration uses the Windows SetupAPI via
# ctypes (src/usb_devices.py), so no USB package is needed.

# ── Audio ─────────────────────────────────────────────────────────────────────
sounddevice>=0.4.6
numpy>=1.24.0

# Provides the FFmpeg binary used to decode SBC/AAC (bundled into the EXE)
imageio-ffmpeg>=0.4.9

# ── GUI ───────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0
Pillow>=10.0.0

# System tray icon (minimize to tray)
pystray>=0.19.4
