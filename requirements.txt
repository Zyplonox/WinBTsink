# Python dependencies for BT-AudioSink
#
# Installation:
#   pip install -r requirements.txt

# ── Bluetooth stack ───────────────────────────────────────────────────────────
# BTstack (C) handles Bluetooth via btstack_sink.exe subprocess.
# Run btstack/build.ps1 to build it (requires MSYS2/MinGW, auto-installed).
# pyusb is kept for USB dongle enumeration in the UI (winusb_installer.py).

# ── Audio ─────────────────────────────────────────────────────────────────────
sounddevice>=0.4.6
numpy>=1.24.0

# Bundles FFmpeg automatically (no winget / manual installation required)
imageio-ffmpeg>=0.4.9

# ── GUI ───────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0
Pillow>=10.0.0

# System tray icon (minimize to tray)
pystray>=0.19.4

# ── Optional ──────────────────────────────────────────────────────────────────
# USB device enumeration (debugging / dongle detection)
pyusb>=1.2.1
