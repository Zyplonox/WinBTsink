# PyInstaller
build/
dist/
*.spec.bak

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Cached Zadig download (placed next to .exe at runtime)
zadig.exe

# User config / bonding keys (written to %APPDATA% at runtime, not source)
config.json
keys.json

# BTstack downloaded source and compiled output (fetched by do_build.sh / build.ps1)
btstack/btstack-src/
btstack/build/

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
